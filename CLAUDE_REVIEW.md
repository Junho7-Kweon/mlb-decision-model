# Claude 2차 검토 요청서

## 프로젝트 목적

MLB 경기의 승리 확률과 사용자가 선택한 베팅 조합의 생존확률을 추정하는
개인용 의사결정 보조 모델이다. 아직 실제 수익성이 검증된 실전 모델이 아니다.

## 이번 변경의 핵심

1. 오픈데이터 전용 정책을 도입했다.
2. Retrosheet, Chadwick Register, NWS, 사용자 직접 입력만 허용한다.
3. MLB StatsAPI, Baseball Savant, 스포츠북 크롤링, The Odds API 및 미검토
   제3자 데이터는 기본 차단한다.
4. Retrosheet ZIP은 공식 도메인, 필수 CSV, ZIP 경로 안전성, SHA-256을 검사한다.
5. z-score 표준화로 무효였던 피처별 상수 배율을 제거했다.
6. 불펜 결측치를 `-4.0`으로 간주하지 않고 `DataQualityError`를 발생시킨다.
7. 합성 배당 ROI는 실전 수익성 근거로 사용하지 않는다고 명시했다.
8. 공공데이터포털 KSPO 경기결과 API를 14일 지연 결과 검증용으로 추가했다.
9. 실시간 배당은 로컬 스냅샷과 확인 입력으로 받고 생존확률/EV를 분리한다.
10. 같은 경기의 복수 마켓은 한 조합에 함께 들어가지 않도록 `event_id`로 제외한다.

## 검토 요청 항목

### P0: 데이터 권리와 정책

- `DATA_SOURCES.md`, `NOTICE.md`, `config/data_sources.json`, `sources.py`가
  서로 모순되지 않는가?
- Retrosheet 지정 고지문과 Chadwick 귀속 조건이 충분히 반영됐는가?
- 차단 출처가 다른 코드 경로로 우회될 가능성이 있는가?
- 공식 출처 URL 검증 방식이 리디렉션, 미러, 서브도메인 문제에 안전한가?

### P0: 데이터 누수

- 앞으로 생성할 모든 학습 행에 `as_of < first_pitch`를 강제하기 위한 구조가
  충분한가?
- 선발, 라인업, 불펜 최근 사용량, BvP가 경기 후 자료를 참조하게 될 위험을
  어떻게 더 강하게 차단해야 하는가?

### P1: 통계 모델

- `features.py`의 선발/불펜/마무리/BvP 계산식에 단위 혼합이나 잘못된 기본값이
  있는가?
- BvP empirical-Bayes 축소 방식과 신뢰도 계산이 타당한가?
- 결측치를 오류로 중단할 항목과 missing indicator로 학습할 항목을 구분해야
  하는가?
- 상관된 2폴 경기, 동일 경기 파생 마켓 등에 독립확률 곱셈을 사용하지 않도록
  어떤 구조가 필요한가?

### P1: Retrosheet 가져오기

- `retrosheet.py`의 ZIP 검사 및 압축해제에 zip bomb, 중복 basename, 심볼릭 링크,
  덮어쓰기 등의 위험이 남아 있는가?
- 공식 CSV의 실제 파일명과 배포 구조에 맞는가?
- `source_version`을 고정 문자열 대신 어떻게 신뢰성 있게 기록해야 하는가?

### P2: 테스트와 운영

- 현재 테스트에서 빠진 실패 사례와 경계값은 무엇인가?
- 실제 배당 없이 Brier score, log loss, calibration을 검증하는 절차가 적절한가?
- 사용자가 직접 입력한 배당을 Git 및 결과물 재배포에서 확실히 제외하는가?

## 실행 방법

Python 3.11 이상과 표준 라이브러리만 필요하다.

```powershell
$env:PYTHONPATH="src"
python -m unittest discover -s tests -v
```

현재 기준 테스트 15개가 통과한다.

## 아직 구현하지 않은 부분

- Retrosheet CSV를 모델 학습 행으로 변환하는 실제 ETL
- `as_of`/`first_pitch`를 이용한 데이터 누수 검증기
- NWS 날씨 수집기
- 실제 과거 배당 데이터셋과 신뢰 가능한 ROI 백테스트
- 조합 간 상관관계를 반영하는 시뮬레이션
- 배당 캡처 OCR과 사람의 교차확인 절차

검토 결과는 심각도(P0/P1/P2), 파일·행 위치, 재현 방법, 구체적인 수정 제안
형식으로 작성해 달라.
